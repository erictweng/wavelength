// Deprecated. The game no longer uses a built-in item bank or AI ratings.
// Players now write their own spectrum each turn and guess a random 1-20 target.
// Kept only to avoid breaking older references; safe to delete.
module.exports = { CATEGORIES: [] };
